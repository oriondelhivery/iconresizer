# Icon Resizer — Fixed SVG Mask

This version fixes masking by drawing the source image first and then applying the SVG as a destination-in mask.
Drop/replace mask.svg in the Mask box to change the mask live.
